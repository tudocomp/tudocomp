# dependencies
if(TDC_DEPS_MISSING)
    MESSAGE(WARNING
    "There are missing dependencies! See above for details. You may use "
    "'make get_deps' to download and install these dependencies locally.")
endif()

